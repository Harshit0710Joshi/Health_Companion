# Copy to .env for local dev.

# On Vercel: Settings → Environment Variables → VITE_API_URL = https://YOUR-BACKEND.onrender.com
# Enable for Production *and* Preview, then Redeploy (Vite embeds this at build time).

# Backend base URL only — no /api suffix.
VITE_API_URL=http://localhost:10000

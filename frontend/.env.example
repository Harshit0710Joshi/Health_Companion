# Copy to .env for local dev. On Vercel, set VITE_API_URL in Project → Settings → Environment Variables.

# Backend base URL only — no /api suffix (frontend adds /api itself)
VITE_API_URL=http://localhost:10000
